# Roadmap Studio

A single-file roadmap planning tool for organizing pillars, workstreams, initiatives, dates, status, owners, and savings/avoidance values.

Open `index.html` in a browser to use it locally. The app stores work in the browser and can export/import Excel workbooks and project JSON files.
